# Migration from the current repository

The new project replaces the previous root `index.html`, `style.css`, `script.js`, image files, video, and duplicated assets in the old `research`, `notes`, and `repos` directories.

Create a backup branch before replacement. The new project already recreates `research`, `notes`, and `repos` as compatibility redirects, so their previous contents should not be copied over the new files.
